import Link from 'next/link';
import { Edit3 } from 'lucide-react';
import { Pool } from 'pg';
const pool=new Pool({connectionString:process.env.DATABASE_URL});
export default async function TrendingNews(){const {rows}=await pool.query(`SELECT a.id,a.title,a.status,a.views,a."publishedAt",c.name AS "categoryName",u.name AS "authorName" FROM "Article" a JOIN "Category" c ON c.id=a."categoryId" JOIN "User" u ON u.id=a."authorId" WHERE a."isTrending"=true ORDER BY a.views DESC,COALESCE(a."publishedAt",a."createdAt") DESC`);return <><header className="admin-top news-top"><div><p className="breadcrumbs">Dashboard / News Management / <span>Trending News</span></p><h1>Trending News</h1><p>Articles currently highlighted as trending.</p></div><Link className="admin-add" href="/admin/news/new">ADD NEWS</Link></header><section className="admin-card news-list"><div className="news-list-title"><div><h2>Trending Articles</h2><p>{rows.length} trending article{rows.length===1?'':'s'}</p></div></div><div className="responsive-table"><table><thead><tr><th>ARTICLE</th><th>CATEGORY</th><th>STATUS</th><th>AUTHOR</th><th>PUBLISHED DATE</th><th>VIEWS</th><th>ACTION</th></tr></thead><tbody>{rows.map(article=><tr key={article.id}><td><div className="article-cell"><p>{article.title}</p></div></td><td><span className="category-pill">{article.categoryName}</span></td><td><span className={`status-pill ${article.status.toLowerCase()}`}>{article.status}</span></td><td>{article.authorName}</td><td>{article.publishedAt?new Date(article.publishedAt).toLocaleString('en-IN',{dateStyle:'medium',timeStyle:'short'}):'Not published'}</td><td>{article.views.toLocaleString()}</td><td><div className="row-actions"><Link href={`/admin/news/${article.id}`} title="Edit article" aria-label="Edit article"><Edit3 /></Link></div></td></tr>)}</tbody></table>{!rows.length&&<p style={{padding:24,color:'#9aa6b0'}}>No articles are marked as trending news yet.</p>}</div></section></>}