import Link from 'next/link'; import type { Article } from '@/lib/news';
export function TopStories({ articles }: { articles: Article[] }) { return <section className="card top-stories"><h2>TOP STORIES</h2>{articles.map((article, index) => <Link href={`/news/${article.slug}`} className="top-item" key={article.id}><span>{index + 1}</span><img src={article.imageUrl || `/images/top-story-${(index % 4) + 1}.png`} alt="" /><div><h3>{article.title}</h3><small>{article.categoryName} · {article.views.toLocaleString()} views</small></div></Link>)}</section>; }