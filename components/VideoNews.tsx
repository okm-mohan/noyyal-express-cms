import {Play} from 'lucide-react';
import type {HomeMedia} from '@/lib/home';
export function VideoNews({videos}:{videos:HomeMedia[]}){return <section className="card videos"><div className="section-title"><h2>VIDEO NEWS</h2><b>VIEW ALL →</b></div><div className="video-grid">{videos.length?videos.map((video,index)=><article className="database-video-card" key={video.id}><div><img src={video.thumbnailUrl||'/images/hero-rain.png'} alt=""/><i><Play/></i></div><h3>{video.title}</h3><small>{new Date(video.createdAt).toLocaleDateString('en-IN',{dateStyle:'medium'})}</small></article>):<p className="database-empty">No video news has been published yet.</p>}</div></section>}