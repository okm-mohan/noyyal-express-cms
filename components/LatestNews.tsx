import Link from 'next/link'; import { Bookmark } from 'lucide-react'; import type { Article } from '@/lib/news';
export function LatestNews({ articles }: { articles: Article[] }) { return <section className="card latest"><div className="section-title"><h2>LATEST NEWS</h2><div className="tabs"><b>ALL</b><span>COIMBATORE</span><span>TAMIL NADU</span><span>INDIA</span><span>WORLD</span></div></div>{articles.map((article, index) => <Link href={`/news/${article.slug}`} className="latest-item" key={article.id}><img src={article.imageUrl || `/images/latest-${(index % 3) + 1}.png`} alt="" /><div><h3>{article.title}</h3><small><b>{article.categoryName.toUpperCase()}</b> · {article.publishedAt ? new Date(article.publishedAt).toLocaleDateString('en-IN', { dateStyle: 'medium' }) : 'Just now'}</small></div><Bookmark /></Link>)}<footer>VIEW MORE NEWS →</footer></section>; }